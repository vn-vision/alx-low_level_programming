#include "main.h"
#include <strings.h>

/**
* _strcmp - compares two strings.
* description: has two parameters
* @s1: string 1
* @s2: string 2
* Return: 0, if s1==s2, -ve if s1 < s2;
*
*/

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
