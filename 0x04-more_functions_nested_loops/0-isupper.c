#include "main.h"

/**
* _isupper - to check for uppercase letters
* @c: value to be checked by function
* Description: checks whether a letter is in lower or uppercase.
* Return: 0 (success)
*
*/

int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
	
	return (0);
}
