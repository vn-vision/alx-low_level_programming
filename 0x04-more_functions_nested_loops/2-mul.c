#include "main.h"

/**
* mul - multiplies two integers
* @a: first integer
* @b: second integer
* Description: the function multiplies a and b
* Return: 0 (success)
*/

int mul(int a, int b)
{
	int k;

	k = a * b;
	putchar(k);

	return (0);
}
