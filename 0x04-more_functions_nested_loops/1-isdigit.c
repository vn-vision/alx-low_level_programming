#include "main.h"

/**
* _isdigit - checks for a digit
* @c: value to be checked by function
* Description: return 1 if its a digit 0 otherwise
*
*/

int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0);
	
	return (0);

}
