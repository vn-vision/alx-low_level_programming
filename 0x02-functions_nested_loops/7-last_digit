#include "main.h"

/**
* main -Entry point
* Return: 0 (success)
* print_last_digit - prints the last digit of a number
*
*/

int print_last_digit(int n)
{
	int lsdgt;

	lsdgt = n%10;

	return (lsdgt);
}
